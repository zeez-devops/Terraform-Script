name: "01-EKS"
on:
  workflow_dispatch:

jobs:
  Plan:
    uses: zeez-devops/Terraform-Script/.github/workflows/aws-terraform.yml@main
    with:
      gh_environment: dev
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}


